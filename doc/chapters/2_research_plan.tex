\section{Project Plan}

\subsection*{Outlook}
\paragraph{The Interdependence of the Three Optimization Tasks}
A deeper analysis reveals a nested, hierarchical relationship between them that must be reflected in the project's modeling strategy.

The "Optimal Configuration" task is at the base of the hierarchy. The choice of a C-rate (0.5C, 0.33C, 0.25C) and a daily cycle limit (1.0, 1.5, 2.0) directly defines the physical constraints of the BESS, such as its maximum charge/discharge power and daily energy throughput. These parameters are fundamental inputs for the "Optimal Operation" model. 

The \textbf{"Optimal Operation"} model forms the core of the analysis. It takes the configuration parameters as given and, based on market price data, calculates the maximum possible annual revenue for that specific BESS configuration in a given country. This revenue figure is the most critical output of the entire project.

Finally, the "Optimal Investment" task sits at the top of the hierarchy. It uses the annual revenue generated by the operational model as its primary input to calculate the 10-year ROI. The optimal country for investment is simply the one that yields the highest ROI.

\paragraph{The Nested Hierarchy of the Three Optimization Tasks}
\begin{enumerate}
    \item \textbf{"Optimal Operation"}: Focus on building a robust and accurate operational optimization model. This model will then be placed within a larger computational framework that systematically iterates through every possible BESS configuration and every target country. 
    \item \textbf{"Optimal Configuration"}: By running the operational model for each combination (e.g., Germany with 0.25C and 1.0 cycle, Germany with 0.5C and 1.5 cycles, Austria with 0.25C and 1.0 cycle, etc.), the model can then populate a complete results matrix.
    \item \textbf{"Optimal Investment"}: Use the annual revenue generated by the operational model as its primary input to calculate the 10-year ROI. The optimal country for investment is simply the one that yields the highest ROI considering WACC and inflation rates.
\end{enumerate}



% \subsection{Plan (see OneNote)}

% % --- PHASE 0 ---
% \section{Phase 0: Foundation \& Discovery (ETA: 3-5 days)}
% \subsection*{Objective}
% To fully internalize the problem domain, dataset intricacies, and technical constraints before coding the core logic. This foundational work is crucial for building a robust and correct solution.

% \subsection*{Action Items}
% \begin{enumerate}[label=\arabic*., wide, labelindent=0pt]
%     \item \textbf{Comprehensive Document Review:}
%     \begin{itemize}[leftmargin=*]
%         \item \code{TechArena2025\_TechnicalGuidelines\_Phase\_I.pdf}:
%         \begin{itemize}
%             \item Memorize BESS parameters (Table 3): Capacity (4.5 MWh), Power (2.25 MW), Efficiency (85\%), Max Cycles (4500).
%             \item Note financial parameters (Table 2): WACC and inflation for each country.
%             \item Note investment cost: €200/kWh (€200,000/MWh).
%         \end{itemize}
%         \item \code{submission\_instructions.pdf}:
%         \begin{itemize}
%             \item Create placeholder files with exact names: \code{...\_Operation.xlsx}, \code{...\_Investment.xlsx}, \code{...\_Configuration.xlsx}.
%             \item List the precise, case-sensitive column headers for each output file.
%         \end{itemize}
%         \item \code{Huawei\_Nuremberg\_Tech\_Arena\_2025\_Openning\_Webinar.pdf}:
%         \begin{itemize}
%             \item Review for high-level context, key dates, and official communication channels.
%         \end{itemize}
%     \end{itemize}

%     \item \textbf{Domain Knowledge Deep Dive:}
%     \begin{itemize}[leftmargin=*]
%         \item \textbf{BESS Fundamentals:} Research State of Charge (SoC), C-Rate, depth of discharge, and cycle degradation.
%         \item \textbf{European Electricity Markets:} Understand energy arbitrage (Day-Ahead) and capacity markets (FCR \& aFRR).
%         \item \textbf{Financial Modeling:} Research Net Present Value (NPV) and the role of a discount rate, especially with varying WACC and inflation.
%     \end{itemize}

%     \item \textbf{Exploratory Data Analysis (EDA) in \code{data\_analysis.ipynb}:}
%     \begin{itemize}[leftmargin=*]
%         \item \textbf{Load \& Preprocess:} Load all data into pandas DataFrames.
%         \item \textbf{Time Series Integrity:} Convert timestamps using \code{pd.to\_datetime}. Align different time resolutions (15-min and 4-hour) to a common 15-minute interval, likely using forward-fill (\code{ffill}).
%         \item \textbf{Price Visualization:} Plot daily, weekly, and yearly price patterns for all markets and countries.
%         \item \textbf{Initial Market Assessment:} Calculate and compare average price and volatility (standard deviation) for each market across all countries.
%     \end{itemize}
% \end{enumerate}


% % --- PHASE 1 ---
% \section{Phase 1: Core Algorithm - Operation Optimization (ETA: 7-10 days)}
% \subsection*{Objective}
% To develop a functional Battery Energy Storage System (BESS) simulation and a baseline optimization algorithm, focusing on a single scenario first (e.g., Germany, default configuration) to ensure correctness.

% \subsection*{Action Items}
% \begin{enumerate}[label=\arabic*., wide, labelindent=0pt]
%     \item Identify the mathematical model of this problem. I.e., clarify the objective and constraint functions. And for these, we need to firstly Identify:
%         \begin{itemize}
%             \item Decision Variables: Charge/Discharge power, FCR/aFRR capacity bids.
%             \item Objective Function: Maximize total revenue from energy arbitrage and ancillary services.
%         \end{itemize}
%     \begin{itemize}[leftmargin=*]
%         \item Create a Python \code{BESS} class with attributes for capacity, power, SoC, efficiency, and state tracking (cycles, throughput).
%         \item Implement methods like \code{charge(megawatts, duration)} and \code{discharge(megawatts, duration)} that account for efficiency losses and respect physical limits.
%     \end{itemize}
    

%     \item \textbf{Implement the Optimization Strategy (\code{main.py}):} [ALL THE FORMULAS AND EQUATIONS BELOW ARE NOT CHECKED YET!!]
%     \begin{itemize}[leftmargin=*]
%         \item \textbf{Heuristic Baseline:} A rule-based approach comparing expected arbitrage profit vs. ancillary service revenue in each time block.
%         \item \textbf{Advanced (Linear Programming) Target:} Use a library like \code{PuLP}.
%         \begin{itemize}
%             \item \textbf{Decision Variables:} For each timestep $t$: $P_{ch}(t), P_{dis}(t), C_{fcr}(t), C_{afrr}(t)$.
%             \item \textbf{Objective Function:}
%             \[
%             \max \sum_{t} \Delta t \Big[
%                 \lambda_{DA}(t) \cdot P_{dis}(t) - \lambda_{DA}(t) \cdot P_{ch}(t) 
%                 + \lambda_{FCR}(t) \cdot C_{fcr}(t) + \lambda_{aFRR}(t) \cdot C_{afrr}(t) 
%             \Big]
%             \]
%             \item \textbf{Constraints (s.t.):}
%             \[
%             \begin{aligned}
%             E_{soc}(t) &= E_{soc}(t-1) + \Delta t \cdot (\eta_{ch} P_{ch}(t) - \frac{1}{\eta_{dis}} P_{dis}(t)) \quad \forall t \\
%             0 \leq E_{soc}(t) & \leq E_{max} \quad \forall t \\
%             0 \leq P_{ch}(t),\; P_{dis}(t) & \leq P_{max} \quad \forall t \\
%             P_{dis}(t) + C_{fcr}(t) + C_{afrr}(t) & \leq P_{max} \quad \forall t
%             \end{aligned}
%             \]
%         \end{itemize}
%     \end{itemize}

%     \item \textbf{Build the Simulation Engine (\code{main.py}):}
%     \begin{itemize}[leftmargin=*]
%         \item Create a main loop iterating through every 15-minute timestep of 2024.
%         \item Inside the loop: call the optimization strategy, execute actions on the BESS model, and log results.
%         \item After the loop, save the aggregated results to \code{TechArena\_Phase1\_Operation.xlsx}.
%     \end{itemize}
% \end{enumerate}


% % --- PHASE 2 ---
% \section{Phase 2: Scaled Analysis - Investment \& Configuration (ETA: 4-6 days)}
% \subsection*{Objective}
% To scale the core algorithm to run across all required scenarios and perform the subsequent financial analysis.

% \subsection*{Action Items}
% \begin{enumerate}[label=\arabic*., wide, labelindent=0pt]
%     \item \textbf{Parameterize the Simulation:}
%     \begin{itemize}[leftmargin=*]
%         \item Refactor the simulation into a function: \code{run\_simulation(country\_data, bess\_config)} that returns the total yearly profit.
%     \end{itemize}
    
%     \item \textbf{Implement Scenario Loops:}
%     \begin{itemize}[leftmargin=*]
%         \item Create a master script with nested loops to call the simulation function for every combination of country and BESS configuration, storing the results.
%     \end{itemize}

%     \item \textbf{Develop the Investment Analysis Module (\code{investment\_analysis.py}):}
%     \begin{itemize}[leftmargin=*]
%         \item Create a function \code{calculate\_roi(...)} that takes yearly profit, WACC, inflation, and investment cost.
%         % \item Calculate the real discount rate $r$: $r = \frac{1 + \text{WACC}}{1 + \text{inflation}} - 1$.
%         \item Forecast 10-year cash flows, growing the initial profit by the inflation rate.
%         % \item Calculate Net Present Value (NPV): $\text{NPV} = \sum_{n=1}^{10} \frac{\text{CashFlow}_{n}}{(1 + r)^n}$.
%         % \item Calculate Return on Investment (ROI): $\text{ROI} = \frac{\text{NPV} - \text{Investment Cost}}{\text{Investment Cost}}$.
%     \end{itemize}

%     \item \textbf{Generate Final Output Files:}
%     \begin{itemize}[leftmargin=*]
%         \item Aggregate all results into pandas DataFrames and save them to \code{...\_Investment.xlsx} and \code{...\_Configuration.xlsx}.
%     \end{itemize}
% \end{enumerate}


% % --- PHASE 3 ---
% \section{Phase 3: Refinement \& Submission (ETA: 2-3 days)}
% \subsection*{Objective}
% To ensure code quality, thoroughly document the approach, and package the final submission for validation.

% \subsection*{Action Items}
% \begin{enumerate}[label=\arabic*., wide, labelindent=0pt]
%     \item \textbf{Code Quality and Documentation:}
%     \begin{itemize}[leftmargin=*]
%         \item Add docstrings to functions and comments to the optimization model.
%         \item Ensure the code is modular and readable.
%         \item Create a complete \code{requirements.txt} file and test it in a clean virtual environment.
%     \end{itemize}

%     \item \textbf{Write the \code{README.md}:}
%     \begin{itemize}[leftmargin=*]
%         \item \textbf{Methodology:} Clearly explain your optimization approach (e.g., Linear Programming model).
%         \item \textbf{Key Findings:} Summarize your main results, such as the most profitable country and the optimal BESS configuration.
%         \item \textbf{Setup and Execution:} Provide clear, step-by-step instructions for running the code.
%     \end{itemize}

%     \item \textbf{Final Validation:}
%     \begin{itemize}[leftmargin=*]
%         \item Run the entire project one last time to generate the three final \code{.xlsx} files.
%         \item Manually check filenames, column headers, and data integrity.
%         \item Create the final \code{TeamName\_TechArena2025\_Phase1.zip} with the correct directory structure.
%     \end{itemize}
% \end{enumerate}



% \subsection{Technical TODO List}

% \subsubsection{Project Setup \& Environment}
% \begin{itemize}
%     \item Create the mandatory project directory structure:
%     \begin{itemize}
%         \item \texttt{main.py} (in the root)
%         \item \texttt{requirements.txt}
%         \item \texttt{README.md}
%         \item \texttt{input/} subdirectory for the provided data file.
%         \item \texttt{output/} subdirectory where result files will be generated.
%     \end{itemize}
%     \item Ensure the script uses Python 3.8 or higher.
%     \item Maintain a \texttt{requirements.txt} file for all external packages (e.g., pandas, openpyxl).
%     \item Implement all file operations using \textbf{relative paths only}.
% \end{itemize}

% \subsection{Data Ingestion \& Preprocessing}
% \begin{itemize}
%     \item The script must read the input data from \texttt{input/TechArena2025\_ElectricityPriceData\_v2.xlsx}.
%     \item Parse all four sheets from the input file:
%     \begin{itemize}
%         \item \texttt{Day-ahead prices}: 15-minute resolution data.
%         \item \texttt{FCR prices}: 4-hour resolution data.
%         \item \texttt{aFRR capacity prices}: 4-hour resolution data.
%         \item \texttt{Data description}: Contains BESS parameters, WACC, and inflation rates.
%     \end{itemize}
% \end{itemize}

% \subsection{Core Algorithm Development (in \texttt{main.py})}
% \begin{itemize}
%     \item The algorithm must be structured to solve the three optimization problems by systematically iterating through all required scenarios.
%     \item \textbf{Operation Optimization Module:}
%     \begin{itemize}
%         \item For a given country and BESS configuration, process the market data for 2024.
%         \item Develop a model to maximize revenue by making optimal charge, discharge, and market-bidding decisions.
%         \item The model must adhere to the physical constraints of the \textbf{Huawei LUNA2000-4.5MWh BESS}.
%     \end{itemize}
%     \item \textbf{Investment \& Configuration Analysis Module:}
%     \begin{itemize}
%         \item Run the operation optimization for every combination of country and BESS configuration.
%         \item Calculate the 10-year ROI for each scenario (investment cost: 200 EUR/kWh).
%         \item The ROI calculation must apply the country-specific WACC and inflation rates.
%     \end{itemize}
% \end{itemize}

% \subsection{Output File Generation}
% \begin{itemize}
%     \item The script must generate exactly three output files in the \texttt{output/} directory (\texttt{.csv} or \texttt{.xlsx}).
%     \item \textbf{\texttt{TechArena\_Phase1\_Configuration.csv}:}
%     \begin{itemize}
%         \item Required columns: \texttt{C-rate}, \texttt{number of cycles}, \texttt{yearly profits [KEUR/MW]}, \texttt{levelized ROI [\%]}.
%     \end{itemize}
%     \item \textbf{\texttt{TechArena\_Phase1\_Investment.csv}:}
%     \begin{itemize}
%         \item Required columns: \texttt{WACC}, \texttt{inflation rate}, \texttt{discount rate}, \texttt{yearly profits}, year-by-year analysis, and \texttt{Levelized ROI}.
%     \end{itemize}
%     \item \textbf{\texttt{TechArena\_Phase1\_Operation.csv}:}
%     \begin{itemize}
%         \item Required columns: \texttt{Timestamp}, \texttt{Stored energy [MWh]}, \texttt{SoC [-]}, \texttt{Charge [MWh]}, \texttt{Discharge [MWh]}, and columns for market activity.
%     \end{itemize}
% \end{itemize}

% \subsection{Final Submission Packaging}
% \begin{itemize}
%     \item Write a clear \texttt{README.md} file explaining the algorithm's methodology.
%     \item Package the entire project into a single ZIP file named \texttt{TeamName\_TechArena2025\_Phase1.zip}.
% \end{itemize}